-- All tests: write tearDown (or teardown after class) with removing concepts from the database. Not possible now because "delete" does not work.

-- Get concepts: add tests for rows and start

-- get concepts: setUp --> setUpBeforeClass with static fields

-- Import works if use JobManager instead of RespurceManager

-- Export

-- Fetch collections

-- fetch institutions

-- see what is left from Menzo/Matej's list

-- autocomplete: there are some todo's left, see comments in the code.



